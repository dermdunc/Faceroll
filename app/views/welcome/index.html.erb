<section>
	<h1>Sign Up</h1>
	<%= form_for (@user) do |form| %>
  		<div><%= form.email_field :email, placeholder: "Email address" %> </div>
  		<div><%= form.password_field :password, placeholder: "Password" %> </div>
  		<div><%= form.submit "Sign up" %> </div>
	<% end %>
</section>